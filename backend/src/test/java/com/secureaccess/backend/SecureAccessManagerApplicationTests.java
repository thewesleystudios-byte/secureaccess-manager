package com.secureaccess.backend;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

@Disabled("Disabled to keep unit tests lightweight and avoid full context complications")
class SecureAccessManagerApplicationTests {
    @Test
    void contextLoads() { }
}
